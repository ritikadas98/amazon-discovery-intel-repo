# Production build (vite build): point the SPA at the deployed Cloud Run backend.
# Overrides the localhost value in .env (which is for `npm run dev` only).
VITE_API_BASE_URL=https://amazon-discovery-34n34tq6za-el.a.run.app
